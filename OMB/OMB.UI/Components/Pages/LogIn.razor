@page "/login"
@inject userListUseCase userListUseCase

<PageTitle>Log In</PageTitle>

<h1>Log In</h1>

<input placeholder="Nombre (obligatorio)" @bind="mail" class="form-control">
<input placeholder="Nombre (obligatorio)" @bind="password" class="form-control">
<button class="btn btn-primary" @onclick="Aceptar">Aceptar</button>

@code {
    private string mail;
    private string password;
    private User usuario;

    void Aceptar() {
        
        //Busco un usuario que tenga el mail y clave ingresados
        usuario = userListUseCase.Execute().Where(user => user.mail == mail && user.password == password).SingleOrDefault();
        
        if (usuario != null) {

            //Si el usuario existe, la sesión iniciada actual debe ser la de ese usuario

        }
    }
}
